<!DOCTYPE html>
<html data-theme="light" lang="en" class="h-full antialiased scroll-smooth">
  <head>
    <title>Watheia Labs | We make technology easy</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>
  <body class="h-full">
    <article>
      <%= render 'blocks/sections/header' %>
      <main>
        <%= yield %>
      </main>
      <%= render 'blocks/sections/footer' %>
    </article>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
  </body>
</html>
